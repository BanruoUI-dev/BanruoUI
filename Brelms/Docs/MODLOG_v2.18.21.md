# Bre v2.18.21 - 输出动作抽屉（UI模板化）

- 输出动作：下拉“动作类型”调整为 入场 / 出场 / 旋转（共用同一UI模板）。
- 新增：接入“新建”按钮，创建动作条（默认展开）。
- 动作条：接入 +/- 折叠/展开（仅UI），并增加 周期循环（UI）。
- 参数区（仅UI展示/编辑本地状态）：周期持续时间(s) / 速率 / 延迟开始(s)。
- 条件下拉：保留“无条件”（占位，暂不接入条件系统/执行链路）。
